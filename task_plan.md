# Task Plan

- Step 1: Add the failing tests for label mapping and stratified split so the requirements are captured.
- Step 2: Implement `SessionRecord`, binary/multiclass label helpers, and the stratified split util following the provided signatures.
- Step 3: Run the targeted pytest command and adjust as needed until it passes.
- Step 4: Review changes, ensure documentation files reflect progress, then prepare for commit.

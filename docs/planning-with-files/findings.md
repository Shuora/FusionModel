# Findings: Canonical Final Metric Handling

## What To Confirm
- 当前 canonical final metric 文件名/路径：`final_metrics.json` vs `meta_metrics.json`
- report discovery 与 stage2 summary 是否指向同一文件
- tests 当前断言的是哪一个文件，以及为什么

## Notes
- (pending) 扫描 `src/report.py`, `src/experiments/stage2_multiclass.py`, `src/train.py` 与 pipeline tests 后补充具体位置与路径。


run_name: fusion_baseline
dataset_name: tls_full
seed: 42
num_epochs: 20
batch_size: 64
output_root: outputs/runs
optimizer:
  name: adamw
  lr: 0.0003
model:
  attention_dim: 256
  num_classes: 10
stagewise:
  - stage1_branch_warmup
  - stage2_fusion_train
  - stage3_pre_stacking
